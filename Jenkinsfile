pipeline {
    agent any
}

// triggers {
//     pollSCM('* * * * *')
// }

stages {
    stage('Build doc') {
        steps {
            sh 'echo "Build doc"'
        }
    }

    stage('Upload doc') {
        steps {
            sh 'echo "Upload doc"'
        }
    }
}
